import { JsonObject, JsonValue } from "./json-typings";
import { JsonReadOptions } from "./json-format-contract";
import { EnumInfo, LongType, MessageInfo, ScalarType } from "./reflection-info";
import { UnknownEnum, UnknownScalar } from "./unknown-types";
/**
 * Reads proto3 messages in canonical JSON format using reflection information.
 *
 * https://developers.google.com/protocol-buffers/docs/proto3#json
 */
export declare class ReflectionJsonReader {
    protected readonly info: MessageInfo;
    /**
     * JSON key to field.
     * Accepts the original proto field name in the .proto, the
     * lowerCamelCase name, or the name specified by the json_name option.
     */
    private fMap?;
    constructor(info: MessageInfo);
    protected prepare(): void;
    assert(condition: any, fieldName: string, jsonValue: JsonValue): asserts condition;
    /**
     * Reads a message from canonical JSON format into the target message.
     *
     * Repeated fields are appended. Map entries are added, overwriting
     * existing keys.
     *
     * If a message field is already present, it will be merged with the
     * new data.
     */
    read<T extends object>(input: JsonObject, message: T, options: JsonReadOptions): void;
    /**
     * google.protobuf.NullValue accepts only JSON `null`.
     */
    enum(type: EnumInfo, json: unknown, fieldName: string): UnknownEnum;
    scalar(json: JsonValue, type: ScalarType, longType: LongType | undefined, fieldName: string): UnknownScalar;
}
